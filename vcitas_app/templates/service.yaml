apiVersion: v1
kind: Service
metadata:
  name: {{ include "vcitas_app.fullname" . }}
  labels:
    {{- include "vcitas_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vcitas_app.selectorLabels" . | nindent 4 }}
